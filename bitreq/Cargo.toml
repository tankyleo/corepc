[package]
name = "bitreq"
version = "0.3.7"
authors = ["Jens Pitkanen <jens@neon.moe>", "Tobin C. Harding <me@tobin.cc>", "Matt Corallo", "Elias Rohrer <dev@tnull.de>"]
description = "Simple, minimal-dependency HTTP client"
documentation = "https://docs.rs/bitreq"
repository = "https://github.com/rust-bitcoin/corepc"
readme = "README.md"
keywords = ["http", "https", "client", "request", "json"]
categories = ["web-programming::http-client"]
license = "ISC"
edition = "2021"
rust-version = "1.75.0"

[dependencies]
# For the json-using-serde feature:
serde = { version = "1.0.101", default-features = false, optional = true }
serde_json = { version = "1.0.0", default-features = false, features = ["std"], optional = true }

# For the proxy feature:
base64 = { version = "0.22", default-features = false, features = ["alloc"], optional = true }

log = { version = "0.4.0", default-features = false, optional = true }

[target.'cfg(not(all(target_arch = "wasm32", any(target_os = "unknown", target_os = "none"))))'.dependencies]
# For rustls-based TLS:
rustls = { version = "0.23.38", default-features = false, features = ["ring", "std", "tls12"], optional = true }
rustls-native-certs = { version = "0.8.3", default-features = false, optional = true }
webpki-roots = { version = "1.0.7", default-features = false, optional = true }
rustls-webpki = { version = "0.103.13", default-features = false, optional = true }

# For native-tls-based TLS:
native-tls = { version = "0.2", default-features = false, optional = true }

# For the async feature:
tokio = { version = "1.0", default-features = false, features = ["rt", "net", "io-util", "time", "sync"], optional = true }
tokio-rustls = { version = "0.26.4", default-features = false, optional = true }
tokio-native-tls = { version = "0.3", default-features = false, optional = true }

[target.'cfg(all(target_arch = "wasm32", target_os = "unknown"))'.dependencies]
js-sys = { version = "0.3", default-features = false, optional = true }
# Keep the wasm-bindgen family compatible with this crate's MSRV.
wasm-bindgen = { version = ">=0.2, <0.2.118", default-features = false, optional = true }
wasm-bindgen-futures = { version = "0.4", default-features = false, optional = true }
web-sys = { version = "0.3", default-features = false, features = ["AbortController", "AbortSignal", "Headers", "ReadableStream", "ReadableStreamDefaultReader", "RequestInit", "Response", "Request", "ServiceWorkerGlobalScope"], optional = true }

[target.'cfg(not(all(target_arch = "wasm32", any(target_os = "unknown", target_os = "none"))))'.dev-dependencies]
tiny_http = "0.12"
tokio = { version = "1.0", default-features = false, features = ["macros", "rt-multi-thread", "time"] }
proptest = { version = "1", default-features = false, features = ["std"] }
url = { version = "2.4" }

[package.metadata.docs.rs]
all-features = true
additional-targets = ["wasm32-unknown-unknown"]

[package.metadata.rbmt.lint]
# Unavoidable TLS/crypto dep-graph version conflicts.
allowed_duplicates = ["base64", "core-foundation", "getrandom", "openssl-probe", "security-framework", "windows-sys"]

[features]
default = ["std"]
std = []

log = ["dep:log"]
json-using-serde = ["serde", "serde_json"]
proxy = ["base64", "std"]

wasm-bindgen = ["std", "dep:js-sys", "dep:wasm-bindgen", "dep:wasm-bindgen-futures", "dep:web-sys"]

https = ["https-rustls"]
https-rustls = ["rustls", "webpki-roots", "rustls-webpki"]
https-rustls-probe = ["rustls", "rustls-native-certs"]
https-native-tls = ["native-tls"]

async = ["tokio", "std"]
async-https = ["async-https-rustls"]
async-https-rustls = ["async", "https-rustls", "tokio-rustls"]
async-https-rustls-probe = ["async", "https-rustls-probe", "tokio-rustls"]
async-https-native-tls = ["async", "https-native-tls", "tokio-native-tls"]

[[example]]
name = "hello"
required-features = ["std"]

[[example]]
name = "iterator"
required-features = ["std"]

[[example]]
name = "async_hello"
required-features = ["async"]

[lints.clippy]
# Allow `format!("{}", x)`instead of enforcing `format!("{x}")`
uninlined_format_args = "allow"

[lints.rust.unexpected_cfgs]
level = "warn"
check-cfg = ["cfg(fuzzing)", "cfg(bitreq_wasm)"]

# vim: ft=conf
